machine:
  environment:
    PROJECT_NAME: review-preview
    CLUSTER_NAME: review-preview-cluster
    CLOUDSDK_COMPUTE_ZONE: asia-east1-a
    DEBIAN_FRONTEND: noninteractive
  services:
    - docker

dependencies:
  pre:
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet components update
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet components update kubectl
    - echo ${GCLOUD_ACCOUNT_JSON} | base64 -d > ${HOME}/gcloud.json
    - sudo /opt/google-cloud-sdk/bin/gcloud auth activate-service-account --key-file ${HOME}/gcloud.json
    - sudo /opt/google-cloud-sdk/bin/gcloud config set project $PROJECT_NAME
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet config set container/cluster $CLUSTER_NAME

deployment:
  prod:
    branch: master
    commands:
      - docker run --rm -v `pwd`/articles:/sg0 \
        gcr.io/review-preview/review-preview \
        /bin/sh -c \
        "cd /sg0 && \
        review-webmaker config.yml && \
        cp --parents /sg0/*.* /usr/share/nginx/html/"
  feature:
    branch: /feature-.*/
    commands:
      - docker run --rm -v `pwd`/articles:/sg0/${CIRCLE_BRANCH} \
        gcr.io/review-preview/review-preview \
        /bin/sh -c \
        "cd /sg0/${CIRCLE_BRANCH} && \
        review-webmaker config.yml && \
        cp --parents /sg0/${CIRCLE_BRANCH}/*.* /usr/share/nginx/html/"
